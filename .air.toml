root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/main.go"
  bin = "tmp/main"
  full_bin = true
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["vendor", "tmp"]
  include_dir = ["cmd", "internal"]